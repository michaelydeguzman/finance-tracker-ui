---
phase: 1003-login-and-signup-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
  - app/(app)/layout.tsx
  - app/(app)/page.tsx
  - app/(app)/income/
  - app/(app)/expenses/
  - app/(app)/categories/
  - app/(app)/households/
  - app/(auth)/layout.tsx
  - app/(auth)/login/page.tsx
  - app/(auth)/signup/page.tsx
  - app/(auth)/components/login-form.tsx
  - app/(auth)/components/signup-form.tsx
  - routes.ts
  - components/header/header.tsx
  - .planning/phases/1003-login-and-signup-pages/1003-UAT.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - Root `app/layout.tsx` wraps children with fonts, `ThemeProvider`, and `Toaster` only — **no** `SidebarProvider`, **no** `Header`, **no** padded `main` wrapper.
    - `app/(app)/layout.tsx` contains the previous shell (`SidebarProvider`, `Header`, padded `main`) so `/`, `/income`, `/expenses`, `/categories`, `/households` unchanged for users.
    - `GET /login` and `GET /signup` render centered auth UI per `1003-UI-SPEC.md` without the app shell.
    - `routes.ts` default import path points at `./app/(app)/page` (or equivalent) so `npm run build` succeeds.
    - `components/header/header.tsx` exposes a navigation control to `/login` (e.g. `Link` with `Button variant="ghost"`).
    - `npm run build` passes.
  artifacts:
    - path: app/(auth)/components/login-form.tsx
      provides: client login form shell (can noop submit until plan 02)
    - path: app/(auth)/components/signup-form.tsx
      provides: client signup form shell
    - path: .planning/phases/1003-login-and-signup-pages/1003-UAT.md
      provides: UAT rows for manual QA
  key_links:
    - from: app/(auth)/login/page.tsx
      to: login-form.tsx
      via: default export page imports form
---

<objective>
Implement **1003-CONTEXT D-04, D-05, D-06, D-09** and **1003-UI-SPEC** layout shell: split root vs `(app)` chrome, add **`(auth)`** segment with **`/login`** and **`/signup`** pages and colocated **client** form components (validation + disabled submit wiring acceptable before plan 02 wires API).

Output: moved route tree under **`app/(app)/`**, new **`app/(auth)/`** routes, **`routes.ts`** fix, header login entry, **`npm run build`** green.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1003-login-and-signup-pages/1003-CONTEXT.md
@.planning/phases/1003-login-and-signup-pages/1003-RESEARCH.md
@.planning/phases/1003-login-and-signup-pages/1003-UI-SPEC.md
@.cursor/rules/finance-tracker-agents.mdc
@app/layout.tsx
@routes.ts
@components/header/header.tsx
@app/transactions/components/add-transaction-dialog.tsx
@components/ui/card.tsx
@components/ui/input.tsx
@components/ui/button.tsx
@components/ui/spinner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Slim root layout + add `(app)` shell layout</name>
  <files>app/layout.tsx, app/(app)/layout.tsx</files>
  <read_first>
    - `app/layout.tsx` (full file)
    - `.planning/phases/1003-login-and-signup-pages/1003-CONTEXT.md` (D-04, D-05)
    - `.planning/phases/1003-login-and-signup-pages/1003-UI-SPEC.md` (auth shell rules)
  </read_first>
  <action>
    1. Replace `app/layout.tsx` body structure so it **only** includes: `html` + `body` with font variables, `ThemeProvider` (same props as today), **`Toaster`** from `@/components/ui/sonner` **inside** `ThemeProvider`, then **`{children}`** — remove `SidebarProvider`, `Header`, and the `main` wrapper with `px-[80px]`.

    2. Create **`app/(app)/layout.tsx`** as a **Server Component** default export that wraps `children` with **exact** prior shell markup: `SidebarProvider` → `main` with classes `w-full flex flex-col min-h-screen justify-center items-center max-w-[1400px] mx-auto` → `Header` → inner `div` with `flex flex-col gap-8 flex-grow w-full px-[80px] py-5` → `{children}`.

    3. Preserve `suppressHydrationWarning` on `html` and existing `className` on `body`.
  </action>
  <acceptance_criteria>
    - `app/layout.tsx` contains the substring `ThemeProvider` and contains `{children}` as the direct themed content (no `SidebarProvider` string in this file).
    - `app/(app)/layout.tsx` exists and contains both substrings `SidebarProvider` and `Header` (import from `@/components/header/header` or equivalent existing path).
    - `app/(app)/layout.tsx` contains the substring `px-[80px]`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Move existing `page.tsx` routes under `(app)`</name>
  <files>app/(app)/page.tsx, app/(app)/income, app/(app)/expenses, app/(app)/categories, app/(app)/households</files>
  <read_first>
    - `routes.ts` (full file — note default import path)
    - `app/page.tsx` (full file)
    - List directories `app/income`, `app/expenses`, `app/categories`, `app/households` before moving
  </read_first>
  <action>
    1. Physically move (git mv or equivalent) these paths under **`app/(app)/`**: `app/page.tsx` → `app/(app)/page.tsx`; entire folders **`app/income`**, **`app/expenses`**, **`app/categories`**, **`app/households`** → `app/(app)/income`, etc. **Do not** move `app/api`, `app/transactions`, `app/globals.css`, or root `app/loading.tsx`.

    2. Update **`routes.ts`** line `import Home from "./app/page";` to **`import Home from "./app/(app)/page";`** (keep variable name `Home` to minimize diff).

    3. Search repo for string **`./app/page`** or **`@/app/page`** imports; fix any broken imports to `./app/(app)/page` or correct `@/` path.
  </action>
  <acceptance_criteria>
    - File exists at path `app/(app)/page.tsx`
    - File exists at path `app/(app)/income/page.tsx`
    - `routes.ts` contains the exact substring `import Home from "./app/(app)/page";`
    - `rg "from [\"']@/app/page"` across repo returns no matches (or only commented lines)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: `(auth)` layout + login/signup routes + forms</name>
  <files>app/(auth)/layout.tsx, app/(auth)/login/page.tsx, app/(auth)/signup/page.tsx, app/(auth)/components/login-form.tsx, app/(auth)/components/signup-form.tsx</files>
  <read_first>
    - `.planning/phases/1003-login-and-signup-pages/1003-UI-SPEC.md` (full)
    - `components/ui/card.tsx` (exports)
    - `app/transactions/components/add-transaction-dialog.tsx` (toast + Input + Spinner patterns)
  </read_first>
  <action>
    1. Add **`app/(auth)/layout.tsx`** (Server Component): outer `div` with `className` including **`min-h-screen`**, **`flex`**, **`items-center`**, **`justify-center`**, **`bg-background`**, and horizontal padding; inner wrapper `w-full max-w-sm` (or `max-w-md` if `1003-UI-SPEC` says sm — use **`max-w-sm`**).

    2. Add **`app/(auth)/login/page.tsx`** (Server Component) that renders a title (e.g. h1 **“Sign in”**) and imports default **`LoginForm`** from `@/app/(auth)/components/login-form`.

    3. Add **`app/(auth)/signup/page.tsx`** similarly with **“Create account”** and **`SignupForm`**.

    4. Implement **`app/(auth)/components/login-form.tsx`** as **`"use client"`**: `Card` with email + password `Input`s, labels with **`htmlFor`**, full-width **`Button type="submit"`** with text **exactly** `Sign in` per UI-SPEC; `useState` for field errors; on submit prevent empty fields; optional: call placeholder `async function onSubmit()` that `toast.info("Coming soon")` from **`sonner`** until plan 02 — **if** using toast, import **`toast`** from **`sonner`**.

    5. Implement **`signup-form.tsx`** as **`"use client"`** with password + confirm; enforce password length **≥ 8** and matching confirm with **inline** error text; submit button text **exactly** `Create account`.

    6. Add **`next/link`** footer links: login form footer → **`/signup`** with visible text containing **“Create an account”**; signup form → **`/login`** with text containing **“Sign in”** (substring match **“Sign in instead”** per UI-SPEC — use that exact phrase for the link label).
  </action>
  <acceptance_criteria>
    - `app/(auth)/login/page.tsx` exists and imports `LoginForm` from `@/app/(auth)/components/login-form` OR relative `./../components/login-form`
    - `app/(auth)/components/login-form.tsx` contains substring `"use client"` and substring `Sign in` (button label)
    - `app/(auth)/components/signup-form.tsx` contains substring `Create account` and substring `8` (min length constant or message)
    - `app/(auth)/components/signup-form.tsx` contains substring `Sign in instead`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: Header entry to `/login` + UAT touch-up</name>
  <files>components/header/header.tsx, .planning/phases/1003-login-and-signup-pages/1003-UAT.md</files>
  <read_first>
    - `components/header/header.tsx` (full file)
    - `.planning/phases/1003-login-and-signup-pages/1003-UAT.md`
  </read_first>
  <action>
    1. In **`components/header/header.tsx`**, add a **`next/link`** to **`/login`** inside the right-side cluster (near `UserHeader`), using **`Button variant="ghost"`** with **`asChild`** **or** `Link` styled as ghost button. Link visible text must include **`Log in`** (exact capitalization).

    2. Ensure **`header.tsx`** remains a Client Component if it already is; if you import `Link`, keep hooks usage valid.

    3. In **`1003-UAT.md`**, set scenario **U8** build row to reflect **`npm run build`** as the command (no text change if already correct); add a row **U9** documenting header **Log in** link if missing.
  </action>
  <acceptance_criteria>
    - `components/header/header.tsx` contains substring `/login` and substring `Log in`
    - `1003-UAT.md` contains substring `npm run build`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>
