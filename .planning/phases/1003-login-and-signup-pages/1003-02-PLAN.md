---
phase: 1003-login-and-signup-pages
plan: 02
type: execute
wave: 1
depends_on:
  - "1003-01-PLAN.md"
files_modified:
  - lib/api/endpoints/auth.ts
  - lib/api/auth.ts
  - app/api/auth/login/route.ts
  - app/api/auth/signup/route.ts
  - app/(auth)/components/login-form.tsx
  - app/(auth)/components/signup-form.tsx
  - .planning/phases/1003-login-and-signup-pages/1003-UAT.md
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - `POST /api/auth/login` and `POST /api/auth/signup` exist as Route Handlers; both validate JSON body; return **`501`** with JSON body containing **`success`** field `false` and a non-empty **`message`** when backend integration is not wired (or when `API_URL` missing), matching patterns readable by `apiFetch` error path.
    - `lib/api/auth.ts` exports async functions that call **`apiFetch`** against **`/api/auth/login`** and **`/api/auth/signup`** with `method: "POST"` and `Content-Type: application/json`.
    - Login and signup forms call these functions on submit; show **`toast.error`** on failure with `Error#message`; show **`toast.success`** only if response indicates success (stub should not succeed).
    - `npm run build` passes.
  artifacts:
    - path: lib/api/endpoints/auth.ts
      provides: relative URL constants for auth BFF
    - path: app/api/auth/login/route.ts
      provides: POST stub / future proxy
  key_links:
    - from: login-form.tsx
      to: lib/api/auth.ts
      via: submit handler
---

<objective>
Implement **1003-CONTEXT D-07, D-08**: BFF **`app/api/auth/*`** stubs and **`lib/api/auth`** client; wire **`LoginForm`** / **`SignupForm`** to **`apiFetch`** with loading state, Sonner errors, and **no** `window.alert`.

Output: working network round-trip to stubs (expect error toast), **`npm run build`** green, **`1003-UAT.md`** U6 satisfied.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1003-login-and-signup-pages/1003-CONTEXT.md
@.planning/phases/1003-login-and-signup-pages/1003-RESEARCH.md
@lib/api/config.ts
@lib/api/transactions.ts
@lib/api/endpoints/transactions.ts
@app/api/transactions/route.ts
@app/(auth)/components/login-form.tsx
@app/(auth)/components/signup-form.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Endpoint constants + `lib/api/auth.ts`</name>
  <files>lib/api/endpoints/auth.ts, lib/api/auth.ts</files>
  <read_first>
    - `lib/api/endpoints/transactions.ts` (full file — mirror export style)
    - `lib/api/transactions.ts` (first 80 lines — `apiFetch` usage)
    - `lib/api/config.ts` (full file)
  </read_first>
  <action>
    1. Create **`lib/api/endpoints/auth.ts`** exporting an object **`AUTH_ENDPOINTS`** with string properties **`login: "/api/auth/login"`** and **`signup: "/api/auth/signup"`** (leading slash, no trailing slash).

    2. Create **`lib/api/auth.ts`** exporting types **`LoginRequest = { email: string; password: string }`** and **`SignupRequest = { email: string; password: string }`** (omit confirm field from wire payload).

    3. Export **`login(request: LoginRequest): Promise&lt;unknown&gt;`** that calls **`apiFetch(AUTH_ENDPOINTS.login, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(request) })`**.

    4. Export **`signup(request: SignupRequest)`** similarly posting to **`AUTH_ENDPOINTS.signup`**.

    5. If backend later returns **`{ success, data }`** envelope, document in file comment that **`apiFetch`** unwraps `data` — stub may return plain JSON; adjust only if `apiFetch` throws incorrectly (goal: **throw** on stub error so UI shows toast).
  </action>
  <acceptance_criteria>
    - `lib/api/endpoints/auth.ts` contains substring `/api/auth/login` and substring `/api/auth/signup`
    - `lib/api/auth.ts` contains substring `apiFetch` and substring `AUTH_ENDPOINTS.login`
    - `lib/api/auth.ts` exports names `login` and `signup` as functions (file contains `export async function login` and `export async function signup`)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Route handlers `POST` login + signup stubs</name>
  <files>app/api/auth/login/route.ts, app/api/auth/signup/route.ts</files>
  <read_first>
    - `app/api/transactions/route.ts` (first 120 lines — `POST` JSON parse, `Response.json` error patterns)
    - `.planning/phases/1003-login-and-signup-pages/1003-CONTEXT.md` (D-07)
  </read_first>
  <action>
    1. Create **`app/api/auth/login/route.ts`** exporting **`export async function POST(request: Request)`**. Read JSON body; if not object or missing **`email`** or **`password`** strings (non-empty after trim), return **`Response.json({ success: false, message: "email and password are required." }, { status: 400 })`**.

    2. For valid body, return **`Response.json({ success: false, message: "Authentication API is not configured." }, { status: 501 })`** (exact **`message`** string).

    3. Create **`app/api/auth/signup/route.ts`** with same pattern; validate **`email`**, **`password`**; optional: reject password shorter than 8 with **`400`** message **`password must be at least 8 characters.`** (exact string).

    4. On valid signup body, return same **`501`** JSON as login with identical **`message`** string as login stub.

    5. Wrap parse in try/catch; on JSON parse failure return **`400`** with **`{ success: false, message: "Invalid JSON body." }`** (exact `message`).
  </action>
  <acceptance_criteria>
    - `app/api/auth/login/route.ts` contains substring `Authentication API is not configured.`
    - `app/api/auth/login/route.ts` contains substring `email and password are required`
    - `app/api/auth/signup/route.ts` contains substring `password must be at least 8 characters` OR signup validates length in handler
    - `app/api/auth/signup/route.ts` contains `501`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Wire forms + loading + toasts</name>
  <files>app/(auth)/components/login-form.tsx, app/(auth)/components/signup-form.tsx, .planning/phases/1003-login-and-signup-pages/1003-UAT.md</files>
  <read_first>
    - `app/(auth)/components/login-form.tsx` (full file after plan 01)
    - `app/(auth)/components/signup-form.tsx` (full file)
    - `lib/api/auth.ts` (full file after task 1)
    - `app/transactions/components/add-transaction-dialog.tsx` (Spinner + submitting pattern)
  </read_first>
  <action>
    1. In **`login-form.tsx`**, after local validation passes, **`setSubmitting(true)`**, **`try`** **`await login({ email: trimmedEmail, password })`**, **`catch (e)`** with **`toast.error`** using **`e instanceof Error ? e.message : "Sign in failed"`**; **`finally`** **`setSubmitting(false)`**. Disable submit button while **`submitting`**; show **`Spinner`** when submitting (same approach as transaction dialog: conditional render next to button text or inside button).

    2. In **`signup-form.tsx`**, before network call ensure confirm matches and length ≥ 8; then **`await signup({ email, password })`** with identical toast error pattern; messages must surface **`Authentication API is not configured.`** when hitting stub.

    3. Remove any **placeholder** `toast.info("Coming soon")` from plan 01 if present.

    4. Update **`1003-UAT.md`** table: mark **U6** expected text to include **`Authentication API is not configured.`** as the stub message.
  </action>
  <acceptance_criteria>
    - `app/(auth)/components/login-form.tsx` contains substring `await login(` OR substring `login({`
    - `app/(auth)/components/login-form.tsx` contains substring `toast.error`
    - `app/(auth)/components/signup-form.tsx` contains substring `await signup(` OR substring `signup({`
    - `1003-UAT.md` contains substring `Authentication API is not configured.`
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>
