---
phase: 1002-dashboard-analytics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/api/transactions/route.ts
  - app/api/transactions/common/utils.ts
  - lib/api/transactions.ts
  - lib/api/endpoints/transactions.ts
  - components/dashboard/dashboard-client.tsx
  - .planning/phases/1002-dashboard-analytics/1002-UAT.md
autonomous: true
requirements:
  - "1002"
user_setup: []

must_haves:
  truths:
    - GET `/api/transactions` accepts optional `from` and `to` query strings, validates them with the same date-parse rules as `isValidTransactionDate`, requires both if either is present, and forwards them to `${API_URL}/v1/transactions` together with existing optional `categoryType`.
    - `getTransactions` accepts an optional `{ from, to }` object and calls `apiFetch` with the correct path including `URLSearchParams`; `getTransactions()` with no args remains valid for unfiltered lists (dashboard `all` preset).
    - `DashboardClient` loads data via `getTransactions` when the period changes, uses `range === null` for the `all` preset without date query params, shows Sonner error toast on failure, and falls back to `createMockDashboardTransactions(mockNow)` with visible status (live vs demo).
    - `npm run build` passes.
  artifacts:
    - path: app/api/transactions/common/utils.ts
      provides: shared validation helpers for list query (e.g. paired from/to checks) if extracted from route for clarity
    - path: app/api/transactions/route.ts
      provides: BFF GET with from/to forwarding
    - path: lib/api/transactions.ts
      provides: list client with optional date filter + RequestInit passthrough for AbortSignal
    - path: components/dashboard/dashboard-client.tsx
      provides: fetch + fallback UX per D-01/D-02
    - path: .planning/phases/1002-dashboard-analytics/1002-UAT.md
      provides: manual verification scenarios for phase 1002
  key_links:
    - from: dashboard-client.tsx
      to: getTransactions
      via: effect keyed on `preset`, `customStartStr`, `customEndStr` builds ISO `from`/`to` from `resolvePeriodRange`
    - from: route.ts GET
      to: backend /v1/transactions
      via: fetch URL includes `from` and `to` when supplied
---

<objective>
Implement **1002-CONTEXT D-01 through D-03** (and supporting UX from **D-07/D-08**): wire the dashboard to **live** transactions via **`from` / `to`** list filtering, with **mock fallback** and **Sonner** on errors. Extend the BFF GET handler and **`getTransactions`** so the UI does not depend on mock-only data when the API is available.

Output: updated route + client + `dashboard-client.tsx`, plus **`1002-UAT.md`**. **`npm run build`** green.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1002-dashboard-analytics/1002-CONTEXT.md
@.planning/phases/1002-dashboard-analytics/1002-RESEARCH.md
@.cursor/rules/finance-tracker-agents.mdc
@app/api/transactions/route.ts
@app/api/transactions/common/utils.ts
@lib/api/transactions.ts
@lib/api/endpoints/transactions.ts
@lib/api/config.ts
@components/dashboard/dashboard-client.tsx
@components/dashboard/date-range.ts
@components/dashboard/mock-transactions.ts
@components/dashboard/aggregates.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: BFF GET — forward `from` and `to`</name>
  <files>app/api/transactions/route.ts, app/api/transactions/common/utils.ts</files>
  <read_first>
    - `app/api/transactions/route.ts` (full GET handler)
    - `app/api/transactions/common/utils.ts` (`isValidTransactionDate`, exports)
    - `.planning/phases/1002-dashboard-analytics/1002-CONTEXT.md` (D-01, D-03)
  </read_first>
  <action>
    1. In GET, read `from` and `to` from `searchParams` in addition to `categoryType`.

    2. Apply this rule: **if either `from` or `to` is non-null, both must be non-null**; otherwise return `Response.json({ error: "from and to must both be provided." }, { status: 400 })`.

    3. When both are non-null, trim and verify each is a valid date string using existing `isValidTransactionDate` (or re-exported helper). If invalid: `400` with `Response.json({ error: "from and to must be valid date strings." }, { status: 400 })`.

    4. Build the backend URL using `URLSearchParams`: append `categoryType` when valid (unchanged behavior). When `from`/`to` are set, append keys exactly `from` and `to` with the trimmed string values (do not reformat if already valid ISO).

    5. `fetch` the composed `${API_URL}/v1/transactions?...` URL; keep existing non-OK and `catch` error handling.
  </action>
  <acceptance_criteria>
    - `app/api/transactions/route.ts` contains the substring `from and to must both be provided`
    - `app/api/transactions/route.ts` uses `URLSearchParams` to build the backend list URL (constructor or `append`)
    - `app/api/transactions/route.ts` GET branch passes `from` and `to` search params to the backend query when both supplied (file contains both `.get("from")` and `.get("to")` and appends them to the outbound request)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Client — `getTransactions` optional date query</name>
  <files>lib/api/transactions.ts, lib/api/endpoints/transactions.ts</files>
  <read_first>
    - `lib/api/transactions.ts` (full file)
    - `lib/api/endpoints/transactions.ts` (full file)
    - `lib/api/config.ts` (`apiFetch` signature — supports `RequestInit`)
  </read_first>
  <action>
    1. Add exported type `TransactionListFromToQuery = { from: string; to: string }` in `lib/api/transactions.ts` (or a small `types` colocated export if the repo prefers).

    2. Change `getTransactions` to: `export async function getTransactions(init?: RequestInit): Promise<Transaction[]>` **OR** overload / optional second parameter: **preferred shape:** `getTransactions(query?: TransactionListFromToQuery, init?: RequestInit): Promise<Transaction[]>` where:
       - When `query` is `undefined`, request URL is `TRANSACTION_ENDPOINTS.list` (no extra query string).
       - When `query` is defined, request URL is `` `${TRANSACTION_ENDPOINTS.list}?${new URLSearchParams({ from: query.from, to: query.to })}` ``.

    3. Update `getTransactionsByType` only if needed so it still compiles (it may continue to use `TRANSACTION_ENDPOINTS.byCategoryType` unchanged).

    4. In `lib/api/endpoints/transactions.ts`, add JSDoc on `list` documenting optional `from`/`to` query for the Next route (mirror style of categories if present).

    5. Forward `init` (e.g. `{ signal }`) to `apiFetch` for cancellation from the dashboard.
  </action>
  <acceptance_criteria>
    - `lib/api/transactions.ts` contains `TransactionListFromToQuery` OR equivalent explicit type name including `from` and `to` as `string`
    - `lib/api/transactions.ts` `getTransactions` function accepts an optional first argument and passes `URLSearchParams` with keys `from` and `to` when that argument is provided
    - `lib/api/transactions.ts` passes the second `RequestInit` argument through to `apiFetch` when supplied (`...init` spread or explicit `signal` forwarding visible in the `apiFetch` call)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Dashboard — fetch, abort, fallback, status copy</name>
  <files>components/dashboard/dashboard-client.tsx</files>
  <read_first>
    - `components/dashboard/dashboard-client.tsx` (full file)
    - `components/dashboard/date-range.ts` (`resolvePeriodRange`, `ResolvedDateRange`)
    - `components/dashboard/mock-transactions.ts` (`createMockDashboardTransactions`)
    - `@/components/ui/sonner` or `@/lib/api/transactions.ts` import path used elsewhere for `toast` (grep project for `import { toast }`)
  </read_first>
  <action>
    1. Remove unconditional `useMemo(() => createMockDashboardTransactions(mockNow), [mockNow])` as the only data source. Replace with `useState<Transaction[]>([])` (import `Transaction` from `@/app/transactions/types/transaction.model`).

    2. Add state for load lifecycle: at minimum `isLoading` boolean and `dataSource` discriminant `'live' | 'fallback'` (initial load may start as loading until first successful fetch or fallback).

    3. Add `useEffect` depending on `[preset, customStartStr, customEndStr, mockNow]`:
       - Compute `range` the same way as today (`resolvePeriodRange` + `parseDateInput`).
       - Create `AbortController`; on cleanup call `abort()`.
       - If `range === null` (`all` preset): call `getTransactions(undefined, { signal })`.
       - Else: call `getTransactions({ from: range.start.toISOString(), to: range.end.toISOString() }, { signal })`.
       - On success: `setTransactions(mapped rows)`, set `dataSource` to `'live'`.
       - On error (only if not aborted): `toast.error` with a short actionable message, `setTransactions(createMockDashboardTransactions(mockNow))`, set `dataSource` to `'fallback'`.

    4. Keep existing `filterTransactionsByRange(filtered, range)` pipeline OR document in a one-line comment that API returns in-range rows — **minimum requirement:** charts must reflect the selected period per D-04–D-06; if API filtering is authoritative, client filter still safe.

    5. Add footer or `CardDescription` line indicating **Loading…**, **Live data**, or **Demo data (API unavailable)** based on `isLoading` + `dataSource`.

    6. Ensure first paint does not crash: handle empty array (zeros / empty charts) until fetch completes.
  </action>
  <acceptance_criteria>
    - `components/dashboard/dashboard-client.tsx` imports `getTransactions` from `@/lib/api/transactions`
    - `components/dashboard/dashboard-client.tsx` contains `AbortController` and `abort()` in effect cleanup
    - `components/dashboard/dashboard-client.tsx` calls `toast.error` in the error path (substring `toast.error` present)
    - `components/dashboard/dashboard-client.tsx` references `createMockDashboardTransactions` only in fallback path (file still contains `createMockDashboardTransactions`)
    - `components/dashboard/dashboard-client.tsx` contains user-visible substring `Demo` or `demo` in status copy for fallback mode
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 4: UAT document — phase 1002 manual scenarios</name>
  <files>.planning/phases/1002-dashboard-analytics/1002-UAT.md</files>
  <read_first>
    - `.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-UAT.md` (format)
    - `.planning/phases/1002-dashboard-analytics/1002-CONTEXT.md` (D-01–D-08)
  </read_first>
  <action>
    Create `.planning/phases/1002-dashboard-analytics/1002-UAT.md` with YAML frontmatter similar to `1001-UAT.md` (`phase: 1002-dashboard-analytics`, `status: draft`).

    Include at least these scenarios (expected + result placeholders `pending`):
    1. **Live data:** Backend + Next running, non-`all` preset — footer indicates live/success path and charts show API-backed totals.
    2. **Fallback:** Stop backend or break `API_URL` — dashboard shows Sonner error and demo-data messaging; charts still render.
    3. **All records preset:** `all` — request goes out without `from`/`to` (verify in Network tab in instructions).
    4. **Custom range:** Change custom dates — new fetch or consistent filtered totals.

    Add a **Summary** section with `total` / `passed` / `pending` counters initialised to zeros.
  </action>
  <acceptance_criteria>
    - File `.planning/phases/1002-dashboard-analytics/1002-UAT.md` exists
    - File contains frontmatter delimiter `---` at top
    - File contains heading `## Tests` or `###` numbered tests
    - File substring `fallback` OR `Demo` appears at least once
    - File substring `all` appears in a test title or expected steps (preset coverage)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>

## PLANNING COMPLETE
