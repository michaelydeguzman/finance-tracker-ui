---
status: complete
phase: 1002-dashboard-analytics
source:
  - 1002-01-SUMMARY.md
  - 1002-02-SUMMARY.md
started: 2026-03-31T00:00:00Z
updated: 2026-03-31T00:00:00Z
---

## Current Test

[testing complete]

## Tests

### 1. Dashboard — live data path
expected: With Next.js dev server and backend reachable (`API_URL` set), open `/`. Select a bounded preset (e.g. Last 3 months). Footer or description indicates live/success path (not demo). Network shows `GET /api/transactions?from=...&to=...` when preset is not `all`. Charts show non-empty or zero totals consistent with API data.
result: pass
note: "Fix: normalized backend enum values in `lib/api/transactions.ts` so income rows are counted correctly."

### 2. Dashboard — fallback when API fails
expected: Stop the backend or set `API_URL` to an invalid host. Reload `/`. A Sonner toast reports failure. Footer indicates demo / fallback data. Charts still render using `createMockDashboardTransactions`.
expected: Stop the backend or set `API_URL` to an invalid host. Reload `/`. A Sonner toast reports failure. Footer indicates API unavailable and charts show no data (zeros / empty state).
result: pending

### 3. Dashboard — all records preset
expected: Select "All records". Network shows `GET /api/transactions` without `from` and `to` query parameters. No crash; charts aggregate full returned list.
result: pass

### 4. Dashboard — custom date range
expected: Choose Custom, set From/To, verify network request uses ISO `from`/`to` matching the range and chart numbers update.
result: pass
note: "Income missing was caused by enum serialization mismatch; normalized `categoryType` + `amount` in `lib/api/transactions.ts`."

### 5. Transactions list query — extended params contract
expected: In DevTools Network (or via a manual fetch), verify the BFF accepts and forwards:

- `categoryIds` as repeated keys (e.g. `?categoryIds=<id>&categoryIds=<id2>`) and returns `400` with `{ error: \"Invalid categoryIds value.\" }` for an invalid UUID.
- `page` and `pageSize` only as a pair; if only one is provided, returns `400` with `{ error: \"page and pageSize must both be provided.\" }`.
result: pass

## Summary

total: 5
passed: 4
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

- truth: "With Next.js dev server and backend reachable (`API_URL` set), open `/`. Select a bounded preset (e.g. Last 3 months). Footer or description indicates live/success path (not demo). Network shows `GET /api/transactions?from=...&to=...` when preset is not `all`. Charts show non-empty or zero totals consistent with API data."
  status: resolved
  reason: "Resolved after normalizing `categoryType` / `amount` in `lib/api/transactions.ts`."
  severity: major
  test: 1
  root_cause: "Backend enum serialization mismatch (e.g. string vs number) caused `CategoryType.Income` comparisons to fail."
  artifacts:
    - lib/api/transactions.ts
  missing: []
  debug_session: ""
- truth: "Stop the backend or set `API_URL` to an invalid host. Reload `/`. A Sonner toast reports failure. Footer indicates API unavailable and charts show no data (zeros / empty state)."
  status: resolved
  reason: "Updated dashboard to use empty state on API failure (no mock fallback)."
  severity: minor
  test: 2
  root_cause: "Product decision changed: demo-data fallback removed per updated 1002-CONTEXT D-02."
  artifacts:
    - components/dashboard/dashboard-client.tsx
    - .planning/phases/1002-dashboard-analytics/1002-CONTEXT.md
  missing: []
  debug_session: ""
- truth: "Select \"All records\". Network shows `GET /api/transactions` without `from` and `to` query parameters. No crash; charts aggregate full returned list."
  status: resolved
  reason: "Resolved after normalizing `categoryType` / `amount` in `lib/api/transactions.ts`."
  severity: major
  test: 3
  root_cause: "Same as Test 1."
  artifacts:
    - lib/api/transactions.ts
  missing: []
  debug_session: ""
- truth: "Choose Custom, set From/To, verify network request uses ISO `from`/`to` matching the range and chart numbers update."
  status: resolved
  reason: "Resolved after normalizing `categoryType` / `amount` in `lib/api/transactions.ts`."
  severity: major
  test: 4
  root_cause: "Same as Test 1."
  artifacts:
    - lib/api/transactions.ts
  missing: []
  debug_session: ""
