---
phase: 1002-dashboard-analytics
plan: 02
type: execute
wave: 2
depends_on:
  - "1002-01-PLAN.md"
files_modified:
  - app/api/transactions/common/utils.ts
  - app/api/transactions/route.ts
  - lib/api/endpoints/transactions.ts
  - lib/api/transactions.ts
autonomous: true
requirements:
  - "1002"
user_setup: []

must_haves:
  truths:
    - GET `/api/transactions` forwards optional **`categoryIds`**, **`page`**, and **`pageSize`** to the backend when present, with validation consistent with backend OpenAPI (invalid UUIDs or non-positive integers → `400` with `{ error: string }`).
    - Client exposes a single list-query builder (e.g. `transactionListUrl` or extended `getTransactions` argument) that can combine `from`/`to`, `categoryType`, `categoryIds`, and paging without dropping existing behavior from plan 01.
    - `npm run build` passes.
  artifacts:
    - path: app/api/transactions/common/utils.ts
      provides: `isUuidString` (or equivalent) and numeric validation for `page`/`pageSize`
    - path: app/api/transactions/route.ts
      provides: validated forwarding for extended list params
    - path: lib/api/endpoints/transactions.ts
      provides: documented URL builder for full list query
    - path: lib/api/transactions.ts
      provides: typed list query + `getTransactions` support for new fields
  key_links:
    - from: route.ts GET
      to: backend list URL
      via: shared helper builds `URLSearchParams` / query string for all supported keys
---

<objective>
Close **1002-CONTEXT** deferred item **D-03** extension and **`<deferred>`** list: add **`categoryIds`**, **`page`**, and **`pageSize`** to the BFF + client list contract so future dashboard filters and large datasets do not require another breaking change. Plan 01 must be done first (`from`/`to` live path stable).

Do **not** add dashboard UI for category multi-select or pagination unless explicitly requested later — this plan is **API surface + validation + client** only.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1002-dashboard-analytics/1002-CONTEXT.md
@.planning/phases/1002-dashboard-analytics/1002-RESEARCH.md
@.planning/phases/1002-dashboard-analytics/1002-01-PLAN.md
@.cursor/rules/finance-tracker-agents.mdc
@app/api/transactions/route.ts
@app/api/transactions/common/utils.ts
@lib/api/transactions.ts
@lib/api/endpoints/transactions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared validation — UUID list + paging integers</name>
  <files>app/api/transactions/common/utils.ts</files>
  <read_first>
    - `app/api/transactions/common/utils.ts` (full file)
    - Backend Swagger `https://localhost:7203/swagger/index.html` or internal OpenAPI notes in CONTEXT for `GET /api/v1/transactions` parameter shapes
  </read_first>
  <action>
    1. Add `isUuidString(value: string): boolean` using a strict pattern: lowercase hex UUID v4-style `xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx` **or** the same regex pattern already used elsewhere in the repo if one exists (grep `uuid`); if none, implement a single canonical regex in this file only.

    2. Add `parsePositiveIntParam(raw: string | null, name: string): number | Response` **or** equivalent that returns `Response.json({ error: \`${name} must be a positive integer.\` }, { status: 400 })` when invalid, otherwise returns `number` ≥ 1.

    3. Add `buildBackendTransactionListSearchParams(input: { categoryType: number | null; from: string | null; to: string | null; categoryIds: string[]; page: number | null; pageSize: number | null }): URLSearchParams` (names may vary) that:
       - Sets `categoryType`, `from`, `to` when non-null (preserve plan 01 behavior).
       - Appends each category id as `categoryIds` (use the **same repeated-key convention the backend expects** — confirm via Swagger: if backend expects `categoryIds=id1&categoryIds=id2`, use multiple `append`; if comma-separated, join — **document the chosen convention in a one-line comment** after reading Swagger).

    4. Export only what `route.ts` needs.
  </action>
  <acceptance_criteria>
    - `app/api/transactions/common/utils.ts` exports a function whose name includes `Uuid` OR file contains `isUuidString` definition
    - `app/api/transactions/common/utils.ts` contains `categoryIds` substring in a comment or in `URLSearchParams` logic
    - `app/api/transactions/common/utils.ts` validates `page` or `pageSize` with `400` response path (substring `page` and `positive` or `invalid` appears near validation)
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: BFF GET — parse and forward extended params</name>
  <files>app/api/transactions/route.ts</files>
  <read_first>
    - `app/api/transactions/route.ts` (GET handler after plan 01)
    - `app/api/transactions/common/utils.ts` (new helpers)
  </read_first>
  <action>
    1. Parse `categoryIds` from incoming request: support **every** `searchParams.getAll("categoryIds")` entry (and reject empty strings).

    2. Validate each id with `isUuidString`; on failure return `400` with `Response.json({ error: "Invalid categoryIds value." }, { status: 400 })` (exact message string).

    3. Parse optional `page` and `pageSize`:
       - If one is supplied without the other, return `400` with `{ error: "page and pageSize must both be provided." }`.
       - If both supplied, validate positive integers via Task 1 helper.

    4. Build backend URL using the Task 1 `URLSearchParams` builder; `fetch` unchanged otherwise.
  </action>
  <acceptance_criteria>
    - `app/api/transactions/route.ts` contains substring `categoryIds`
    - `app/api/transactions/route.ts` contains substring `page and pageSize must both be provided`
    - `app/api/transactions/route.ts` uses `getAll("categoryIds")` OR documents equivalent parsing for repeated keys
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Client — typed full list query + `getTransactions`</name>
  <files>lib/api/transactions.ts, lib/api/endpoints/transactions.ts</files>
  <read_first>
    - `lib/api/transactions.ts` (after plan 01)
    - `lib/api/endpoints/transactions.ts`
  </read_first>
  <action>
    1. Define `TransactionListQuery` with optional fields: `from`, `to`, `categoryType`, `categoryIds` (string array), `page`, `pageSize` (numbers). Use `exactOptionalPropertyTypes`-safe patterns (omit keys rather than `undefined`).

    2. Implement `transactionListUrl(query?: TransactionListQuery): string` in `lib/api/endpoints/transactions.ts` that returns `/api/transactions` with correctly encoded params (reuse same `categoryIds` encoding as BFF expects).

    3. Change `getTransactions` to accept `TransactionListQuery` as the first argument (union with plan 01 shape or supersede it) and call `apiFetch(transactionListUrl(query), init)`.

    4. Keep `getTransactionsByType` working: either implement via `transactionListUrl({ categoryType: type })` or leave as thin wrapper — **must not duplicate conflicting query encoding**.

    5. Export `TransactionListQuery` for future dashboard hooks.
  </action>
  <acceptance_criteria>
    - `lib/api/endpoints/transactions.ts` exports or defines `transactionListUrl` function
    - `lib/api/transactions.ts` contains type `TransactionListQuery` with `categoryIds` optional array field
    - `lib/api/transactions.ts` `getTransactions` invokes `transactionListUrl` **OR** builds identical query string via shared helper imported from endpoints
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
</task>

</tasks>

## PLANNING COMPLETE
