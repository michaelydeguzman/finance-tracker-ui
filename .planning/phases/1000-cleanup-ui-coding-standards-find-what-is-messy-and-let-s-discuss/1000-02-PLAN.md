---
phase: 1000-cleanup-ui-coding-standards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/copilot-instructions.md
  - .cursor/rules/finance-tracker-agents.mdc
autonomous: true
requirements:
  - "1000"
user_setup: []

must_haves:
  truths:
    - Copiot instructions no longer forbid all manual edits to components/ui; they match Phase 1000 D-05 (extend shadcn consistently).
    - Cursor always-apply agent rule documents server/client default, colocation, semantic Tailwind tokens, Toaster path.
  artifacts:
    - .github/copilot-instructions.md
    - .cursor/rules/finance-tracker-agents.mdc
  key_links:
    - Agent docs reference aligns with 1000-CONTEXT.md
---

<objective>
Align **agent-facing documentation** with locked Phase 1000 decisions **D-01–D-07, D-05** so Copilot/Cursor do not contradict implementation (especially `components/ui/` and Tailwind tokens).

Purpose: Prevents future PRs from reverting standards.
Output: Two updated markdown files + green build (no code logic change required, but run build to confirm repo still clean).
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/1000-cleanup-ui-coding-standards-find-what-is-messy-and-let-s-discuss/1000-CONTEXT.md
@.github/copilot-instructions.md
@.cursor/rules/finance-tracker-agents.mdc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update copilot-instructions — components/ui policy</name>
  <files>.github/copilot-instructions.md</files>
  <read_first>
    - `.github/copilot-instructions.md` (Component Library / File Organization section)
    - `1000-CONTEXT.md` decisions D-03–D-07, D-05
  </read_first>
  <action>
    Replace the line (under shared `ui/` bullet) that states shadcn primitives are auto-generated and **"do not manually edit"** with text that matches **D-05**:
    - `components/ui/` holds shadcn-style primitives; **prefer adding/updating via shadcn CLI** when possible; **manual edits are allowed** when they follow existing patterns (`cn`, `data-slot`, Radix).
    - Keep the bullet list structure; do not remove `shared/` / `layout/` guidance.
    Optionally add one line: prefer semantic Tailwind tokens (`bg-background`, `text-muted-foreground`, …) per project rules — **D-06** (short, one sentence).
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "do not manually edit" .github/copilot-instructions.md` returns no match (string removed).
    - `.github/copilot-instructions.md` contains the phrase `components/ui` and mentions shadcn CLI or "shadcn" and allows intentional extension / patterns (`cn` or `data-slot` or Radix).
  </acceptance_criteria>
  <done>
    Copilot instructions reflect D-05; grep confirms old prohibition removed.
  </done>
</task>

<task type="auto">
  <name>Task 2: Extend finance-tracker-agents.mdc with Phase 1000 must-have bullets</name>
  <files>.cursor/rules/finance-tracker-agents.mdc</files>
  <read_first>
    - `.cursor/rules/finance-tracker-agents.mdc`
  </read_first>
  <action>
    Append a new `## Phase 1000 standards (locked)` section (or equivalent short heading) with **bullet points only** (no more than ~12 lines):
    - Server Components by default; `"use client"` only when needed (**D-01–D-02**).
    - Route colocation under `app/<route>/…`; shared in `components/shared/` and `components/layout/`; no `common/` (**D-03–D-04**).
    - Prefer semantic Tailwind tokens + `cn()` (**D-06–D-07**).
    - Global `Toaster` from `@/components/ui/sonner` inside `ThemeProvider` (**D-09–D-10**).

    Do not duplicate entire CONTEXT file — summary for agents only.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `.cursor/rules/finance-tracker-agents.mdc` contains the substring `@/components/ui/sonner`.
    - Same file contains `Server Components` or `use client` guidance.
    - Same file contains `semantic` or `tokens` (Tailwind) reference.
  </acceptance_criteria>
  <done>
    Agent rule file updated; build still passes.
  </done>
</task>

</tasks>
