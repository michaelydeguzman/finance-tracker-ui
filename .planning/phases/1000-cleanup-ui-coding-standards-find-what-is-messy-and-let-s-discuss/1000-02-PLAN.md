---
phase: 1000-cleanup-ui-coding-standards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/copilot-instructions.md
  - AGENTS.md
autonomous: true
requirements:
  - ROADMAP-1000
user_setup: []
must_haves:
  truths:
    - Agent-facing docs state that `components/ui` may be extended intentionally and that global Toaster uses the shadcn wrapper.
  artifacts:
    - `.github/copilot-instructions.md` updated under Component Library / File Organization to align with phase 1000 CONTEXT.
    - `AGENTS.md` mentions Toaster/theme pattern if not already explicit.
  key_links:
    - Decisions D-03–D-07 and D-05 (shadcn primitives) reflected in prose.
---

<objective>
Align **AGENTS.md** and **`.github/copilot-instructions.md`** with locked decisions **D-03–D-07** and **D-05** (colocation, semantic Tailwind tokens, `cn()`, shadcn `components/ui` as intentional design-system layer).

Purpose: Remove friction where Copilot text says “do not manually edit” `components/ui/` while the repo legitimately extends shadcn primitives (per CONTEXT override note).
Output: Doc edits only; no app behavior change.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/1000-cleanup-ui-coding-standards-find-what-is-messy-and-let-s-discuss/1000-CONTEXT.md
@AGENTS.md
@.github/copilot-instructions.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copilot instructions — components/ui policy</name>
  <read_first>
    - `.github/copilot-instructions.md` (full file, especially **Component Library Usage** and **File Organization**)
    - `1000-CONTEXT.md` canonical_refs and D-05
  </read_first>
  <files>.github/copilot-instructions.md</files>
  <action>
    Replace or amend any statement that **forbids all manual edits** to `components/ui/` with wording that matches **D-05**:
    - Prefer adding/updating primitives with **shadcn CLI** when possible for structural parity.
    - **Intentional edits and extensions** are allowed when they follow existing patterns (`cn`, `data-slot`, Radix, project TypeScript strict rules).

    Add one short bullet: **Global toasts**: use `Toaster` from `@/components/ui/sonner` in root layout inside `ThemeProvider` (reference only—implementation is Plan 1000-01).

    Add one short bullet: **Tailwind**: prefer semantic tokens (`bg-background`, `text-muted-foreground`, …) per **D-06**; use `cn()` for conditional classes per **D-07**.
  </action>
  <verify>
    <automated>findstr /i /c:"components/ui" .github\copilot-instructions.md</automated>
  </verify>
  <acceptance_criteria>
    - `.github/copilot-instructions.md` contains the phrase `shadcn` and explicitly allows **intentional** extension or editing of `components/ui` patterns (semantic match: file must not claim “never manually edit” without qualification).
    - File mentions **semantic** theme tokens or lists at least two of `bg-background`, `text-muted-foreground`, `border-border`.
    - File mentions `cn()` or `lib/utils` for class merging.
  </acceptance_criteria>
  <done>
    Copilot instructions consistent with phase 1000 CONTEXT; no contradictory “never touch ui/” blanket rule.
  </done>
</task>

<task type="auto">
  <name>Task 2: AGENTS.md — cross-reference UI standards</name>
  <read_first>
    - `AGENTS.md`
    - `.github/copilot-instructions.md` (after Task 1 edits)
  </read_first>
  <files>AGENTS.md</files>
  <action>
    Under **How agents should edit code** → UI bullet list, add **one** concise bullet that points to:
    - Root layout must use **themed** Sonner (`@/components/ui/sonner`) **inside** `ThemeProvider` (traceable to Plan 1000-01).
    - Prefer **semantic Tailwind tokens** and `cn()` (pointer to copilot-instructions for detail).

    Keep the section short; avoid duplicating full CONTEXT.
  </action>
  <verify>
    <automated>findstr /i /c:"sonner" AGENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md` contains substring `components/ui/sonner` OR `@/components/ui/sonner`.
    - `AGENTS.md` mentions `ThemeProvider` in the same paragraph or adjacent sentence as Toaster/Sonner.
  </acceptance_criteria>
  <done>
    AGENTS.md reflects global toast + theme expectation for agents.
  </done>
</task>

</tasks>

<verification>
Read both files for internal consistency; no broken markdown headers.
</verification>

<success_criteria>
Docs match D-03–D-07 and D-05; Plan 1000-01 remains the code source of truth for layout structure.
</success_criteria>

<output>
After completion, create `1000-02-SUMMARY.md` in the phase directory.
</output>
