---
phase: 1000-cleanup-ui-coding-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
autonomous: true
requirements:
  - ROADMAP-1000
user_setup: []
must_haves:
  truths:
    - Global Toaster uses design-system wrapper and lives under ThemeProvider so useTheme works in Sonner.
  artifacts:
    - app/layout.tsx imports Toaster from "@/components/ui/sonner" and nests it inside ThemeProvider.
  key_links:
    - ThemeProvider wraps SidebarProvider, Header, and children; Toaster is a direct child of ThemeProvider (same level as SidebarProvider or inside it—either is fine as long as Toaster is descendant of ThemeProvider).
---

<objective>
Fix root layout so global toasts match **D-09** and **D-10** from `1000-CONTEXT.md`: use `@/components/ui/sonner` and render `<Toaster />` **inside** `ThemeProvider`.

Purpose: Toasts must respect `next-themes` and semantic CSS variables defined in `components/ui/sonner.tsx`.
Output: Updated `app/layout.tsx` only.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/1000-cleanup-ui-coding-standards-find-what-is-messy-and-let-s-discuss/1000-CONTEXT.md
@app/layout.tsx
@components/theme-provider.tsx
@components/ui/sonner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout — themed Toaster inside ThemeProvider</name>
  <read_first>
    - `app/layout.tsx` (current import order and component tree)
    - `components/ui/sonner.tsx` (default export name `Toaster`)
    - `components/theme-provider.tsx` (client boundary)
    - `.planning/phases/1000-cleanup-ui-coding-standards-find-what-is-messy-and-let-s-discuss/1000-CONTEXT.md` (D-08–D-10)
  </read_first>
  <files>app/layout.tsx</files>
  <action>
    Per **D-09**: Replace `import { Toaster } from "sonner"` with `import { Toaster } from "@/components/ui/sonner"`.

    Per **D-10**: Remove the standalone `<Toaster />` that sits **above** `<ThemeProvider>`. Render `<Toaster position="bottom-center" />` as a **descendant** of `<ThemeProvider>` (recommended: immediately inside `<ThemeProvider>`, before `<SidebarProvider>`, so Theme + Toaster are both outer).

    Do **not** change `ThemeProvider` props (`attribute="class"`, `defaultTheme="light"`, `enableSystem`, `disableTransitionOnChange`).

    **D-11 (optional in this task):** Add `suppressHydrationWarning` on `<html>` only if you verify theme flash in browser and next-themes docs recommend it; otherwise skip and leave `<html lang="en">` unchanged.
  </action>
  <verify>
    <automated>npm run build (from repo root; must exit 0)</automated>
  </verify>
  <acceptance_criteria>
    - `app/layout.tsx` contains the exact substring `from "@/components/ui/sonner"` (or `from '@/components/ui/sonner'`).
    - `app/layout.tsx` does **not** contain `from "sonner"` (Toaster import).
    - Grep: `<Toaster` appears **after** `<ThemeProvider` opening tag in file order (Toaster is nested inside ThemeProvider’s children tree, not a sibling above ThemeProvider).
    - `npm run build` exits with code 0.
  </acceptance_criteria>
  <done>
    Production build passes; Toaster is from `@/components/ui/sonner` and is rendered inside `ThemeProvider`.
  </done>
</task>

</tasks>

<verification>
Run `npm run build`. Manually smoke: start dev server, trigger any `toast.*` call and confirm toast renders without console errors from `useTheme`.
</verification>

<success_criteria>
Layout matches D-09/D-10; no regression in app shell structure.
</success_criteria>

<output>
After completion, create `.planning/phases/1000-cleanup-ui-coding-standards-find-what-is-messy-and-let-s-discuss/1000-01-SUMMARY.md`
</output>
