---
phase: 1000-cleanup-ui-coding-standards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/layout.tsx
autonomous: true
requirements:
  - "1000"
user_setup: []

must_haves:
  truths:
    - Root layout uses Toaster from @/components/ui/sonner only (no bare "sonner" Toaster import).
    - Toaster is a descendant of ThemeProvider so useTheme() works in components/ui/sonner.tsx.
    - Production build succeeds.
  artifacts:
    - app/layout.tsx updated
  key_links:
    - ThemeProvider wraps app shell; Toaster inside ThemeProvider
---

<objective>
Fix global theming + toast wiring per **1000-CONTEXT.md D-08–D-11**: use the shadcn Sonner wrapper and correct provider order so `next-themes` resolves in toasts. Apply standard `suppressHydrationWarning` on `<html>` when using class-based theming to avoid hydration mismatch warnings.

Purpose: Unblocks correct dark/light toast styling and aligns with locked decisions.
Output: One-file layout change + green `npm run build`.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/1000-cleanup-ui-coding-standards-find-what-is-messy-and-let-s-discuss/1000-CONTEXT.md
@app/layout.tsx
@components/theme-provider.tsx
@components/ui/sonner.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Root layout — ThemeProvider order + shadcn Toaster + html suppressHydrationWarning</name>
  <files>app/layout.tsx</files>
  <read_first>
    - `app/layout.tsx` (full file)
    - `components/ui/sonner.tsx` (confirm default export name `Toaster` and `useTheme` usage)
    - `components/theme-provider.tsx`
  </read_first>
  <action>
    Per **D-09, D-10, D-11** in `1000-CONTEXT.md`:

    1. Remove `import { Toaster } from "sonner";` (or `'sonner'`).
    2. Add `import { Toaster } from "@/components/ui/sonner";` (use project alias `@/`).
    3. Restructure JSX so the tree is:
       - `&lt;html lang="en" suppressHydrationWarning&gt;` — add `suppressHydrationWarning` to the **html** element only (Next.js + next-themes pattern for class-based theme).
       - `&lt;body&gt;` unchanged except remove the duplicate Toaster above ThemeProvider.
       - `&lt;ThemeProvider attribute="class" defaultTheme="light" enableSystem disableTransitionOnChange&gt;` wraps everything that should be themed, including:
         - `&lt;Toaster position="bottom-center" /&gt;` as **first child** inside ThemeProvider (before `SidebarProvider` is fine).
         - Existing `SidebarProvider` + main shell unchanged in order after Toaster.

    4. Do **not** duplicate Toaster — exactly one instance.
    5. Keep all other layout props/metadata imports unchanged.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "from [\"']sonner[\"']"` in `app/layout.tsx` returns no Toaster import (Sonner **component** may still be used elsewhere; layout must not import `Toaster` from `"sonner"`).
    - `app/layout.tsx` contains the substring `@/components/ui/sonner`.
    - `app/layout.tsx` contains `suppressHydrationWarning` on `<html`.
    - File content order: opening `ThemeProvider` appears **before** first `Toaster` reference (Toaster is inside ThemeProvider).
  </acceptance_criteria>
  <done>
    Build passes; Toaster is inside ThemeProvider; layout uses shadcn Sonner path only; html has suppressHydrationWarning per criteria above.
  </done>
</task>

</tasks>
