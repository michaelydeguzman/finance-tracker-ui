---
phase: 1001-complete-income-and-expense-transaction-crud
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - app/income/components/income-list.tsx
  - app/expenses/components/expense-list.tsx
  - app/income/components/income-client.tsx
  - app/expenses/components/expense-client.tsx
autonomous: true
requirements:
  - "1001"
user_setup: []

must_haves:
  truths:
    - Each income and expense row exposes Edit and Delete affordances that call parent callbacks with stable transaction id.
    - Edit opens the shared dialog with mode edit and full Transaction seed from hook state.
    - Delete opens DeleteTransactionAlert; API runs only after Confirm.
    - Row hover styling on touched lists uses semantic token (e.g. hover:bg-muted/60), not hover:bg-gray-100.
  artifacts:
    - path: app/income/components/income-client.tsx
      provides: Dialog state + updateIncomeTransaction + deleteIncomeTransaction wiring
    - path: app/expenses/components/expense-client.tsx
      provides: Same for expenses
    - path: app/income/components/income-list.tsx
      provides: Row actions + tokenized hover
    - path: app/expenses/components/expense-list.tsx
      provides: Row actions + tokenized hover
  key_links:
    - from: income-list.tsx
      to: income-client.tsx
      via: onEditEntry(id) / onDeleteEntry(id) or equivalent props
    - from: income-client.tsx
      to: AddTransactionDialog + DeleteTransactionAlert
      via: state for open + selected Transaction
    - from: income-client.tsx
      to: useIncomeTransactions
      via: updateIncomeTransaction, deleteIncomeTransaction
---

<objective>
Wire **edit** and **delete** flows on **`/income`** and **`/expenses`** using artifacts from **1001-01** (including **transaction API routes + `lib/api/transactions`** aligned with the category pattern per **D-06**): row actions on lists, client state for dialogs, **`update*Transaction`** / **`delete*Transaction`** from hooks hitting `/api/transactions/:id`, and **semantic hover** on list rows per **1001-UI-SPEC**.

Purpose: Delivers D-01/D-02 end-to-end on both routes without duplicating forms.
Output: Updated four client/list files; `npm run build` green.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-CONTEXT.md
@.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-UI-SPEC.md
@app/income/components/income-client.tsx
@app/expenses/components/expense-client.tsx
@app/income/components/income-list.tsx
@app/expenses/components/expense-list.tsx
@app/transactions/components/add-transaction-dialog.tsx
@app/transactions/components/delete-transaction-alert.tsx
@app/income/hooks/use-income-transactions.ts
@app/expenses/hooks/use-expense-transactions.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: IncomeList and ExpenseList — row actions + semantic hover</name>
  <files>app/income/components/income-list.tsx, app/expenses/components/expense-list.tsx</files>
  <read_first>
    - `app/income/components/income-list.tsx` (full file)
    - `app/expenses/components/expense-list.tsx` (full file)
    - `components/ui/button.tsx` (variants, icon button sizing)
  </read_first>
  <action>
    1. Extend props (optional callbacks so typecheck passes if unused elsewhere):
       - `onEditEntry?: (id: string) => void`
       - `onDeleteEntry?: (id: string) => void`

    2. Replace the row container class that uses `hover:bg-gray-100` with **`hover:bg-muted/60`** (or equivalent semantic token from UI-SPEC). Keep `rounded-lg`, `border-border` if present; use `cn()` if adding conditional classes.

    3. Add a trailing **actions** region per row: at minimum **Edit** and **Delete** controls as `Button` `type="button"` `variant="ghost"` `size="icon"` with `aria-label` on each (e.g. `Edit transaction`, `Delete transaction`). Use lucide icons consistent with the page (e.g. `Pencil`, `Trash2`). Call `onEditEntry?.(entry.id)` / `onDeleteEntry?.(entry.id)`; stop event propagation on clicks so nested buttons do not trigger row-level handlers.

    4. Remove misleading **`cursor-pointer`** from the whole row unless the row itself is intentionally clickable; with discrete buttons, prefer default cursor on the row or `cursor-default`.

    5. Ensure **focus-visible** rings: use `Button` defaults or add `focus-visible:ring-*` via shadcn button (no raw gray-*).

    6. Mobile: actions remain visible (UI-SPEC allows always visible on mobile); do not hide behind hover-only menu unless you add a `md:` breakpoint that matches spec "visible on hover or always visible on mobile".
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "hover:bg-gray-100" app/income/components/income-list.tsx app/expenses/components/expense-list.tsx` returns no matches.
    - `app/income/components/income-list.tsx` contains `onEditEntry`, `onDeleteEntry`, and `hover:bg-muted`.
    - `app/expenses/components/expense-list.tsx` contains `onEditEntry`, `onDeleteEntry`, and `hover:bg-muted`.
  </acceptance_criteria>
  <done>
    Both lists expose edit/delete callbacks and use semantic hover token on the touched row container.
  </done>
</task>

<task type="auto">
  <name>Task 2: IncomeClient and ExpenseClient — wire dialogs and hooks</name>
  <files>app/income/components/income-client.tsx, app/expenses/components/expense-client.tsx</files>
  <read_first>
    - `app/income/components/income-client.tsx` (full file)
    - `app/expenses/components/expense-client.tsx` (full file)
    - `app/transactions/components/add-transaction-dialog.tsx` (props from plan 01)
    - `app/transactions/components/delete-transaction-alert.tsx`
    - `app/transactions/types/transaction.model.ts`
  </read_first>
  <action>
    **IncomeClient**

    1. Destructure `updateIncomeTransaction` and `deleteIncomeTransaction` from `useIncomeTransactions()` (already returned by hook file).

    2. State: `editOpen`, `deleteOpen` booleans; `selectedTransaction: Transaction | null` for edit; `pendingDeleteId: string | null` and optionally cache `pendingDeleteName` or derive from `incomeTransactions.find`.

    3. Pass to `IncomeList`: `onEditEntry={(id) => { const t = incomeTransactions.find(x => x.id === id); if (t) { setSelected(t); setEditOpen(true); } }}`, `onDeleteEntry={(id) => { setPendingDeleteId(id); setDeleteOpen(true); }}`.

    4. Render **second** `AddTransactionDialog` instance **or** single dialog with controlled `mode`:
       - Recommended: one dialog with `open={addOpen || editOpen}` — separate state is clearer: keep **add** dialog as today (`addOpen`); add **edit** dialog with `open={editOpen}`, `mode="edit"`, `transaction={selectedTransaction}`, `onUpdate={(id, input) => { updateIncomeTransaction(id, input); setEditOpen(false); setSelected(null); }}`, `onOpenChange` clears selection when closed.

    5. Render `DeleteTransactionAlert` with `open={deleteOpen}`, `transactionName={...}`, `onConfirm={() => { if (pendingDeleteId) deleteIncomeTransaction(pendingDeleteId); setDeleteOpen(false); setPendingDeleteId(null); }}`, `onOpenChange` resets pending id when dismissed.

    **ExpenseClient**

    6. Mirror steps 1–5 with `useExpenseTransactions`, `ExpenseList`, `CategoryType.Expense`, `updateExpenseTransaction`, `deleteExpenseTransaction`.

    7. Import `Transaction` type from `@/app/transactions/types/transaction.model` (or relative path consistent with income file).

    8. Do not wire export toasts or deferred backlog features.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/income/components/income-client.tsx` contains `updateIncomeTransaction` and `DeleteTransactionAlert`.
    - `app/expenses/components/expense-client.tsx` contains `updateExpenseTransaction` and `DeleteTransactionAlert`.
    - `app/income/components/income-client.tsx` passes `onEditEntry` and `onDeleteEntry` to `IncomeList` (substring `onEditEntry`).
    - `app/expenses/components/expense-client.tsx` contains substring `onEditEntry` and substring `onDeleteEntry` where `ExpenseList` is rendered (both callbacks passed).
  </acceptance_criteria>
  <done>
    Both routes open edit dialog with seeded transaction, delete only after AlertDialog confirm, and compile cleanly.
  </done>
</task>

</tasks>

<verification>
Manual smoke (executor documents in SUMMARY): on `/income` and `/expenses`, edit saves and list updates; delete confirms once; no browser `confirm` dialog appears.
</verification>

<success_criteria>
Full CRUD UX is reachable on both pages per CONTEXT.md; list styling matches UI-SPEC token rule for touched rows.
</success_criteria>

<output>
After completion, create `.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-02-SUMMARY.md`
</output>
