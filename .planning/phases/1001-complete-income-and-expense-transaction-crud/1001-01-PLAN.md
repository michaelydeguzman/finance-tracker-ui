---
phase: 1001-complete-income-and-expense-transaction-crud
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/api/transactions.ts
  - lib/api/endpoints/transactions.ts
  - app/api/transactions/common/utils.ts
  - app/api/transactions/route.ts
  - app/api/transactions/[id]/route.ts
  - package.json
  - package-lock.json
  - components/ui/alert-dialog.tsx
  - app/transactions/hooks/use-transactions.ts
  - app/transactions/components/add-transaction-dialog.tsx
  - app/transactions/components/delete-transaction-alert.tsx
autonomous: true
requirements:
  - "1001"
user_setup: []

must_haves:
  truths:
    - Transaction PUT/DELETE Next.js routes and lib/api client methods align with the category reference (per 1001-CONTEXT D-06); shared validators live in app/api/transactions/common/utils.ts.
    - Delete no longer uses browser confirm in the transaction hook; UI can call delete without a second prompt.
    - Validation errors in useTransactions use Sonner toasts, not window.alert.
    - shadcn AlertDialog primitive exists at components/ui/alert-dialog.tsx and builds.
    - Add dialog supports edit mode with correct titles and calls update path with TransactionInput-shaped payload including categoryName and createdBy.
    - A reusable delete confirmation component uses AlertDialog and destructive confirm button.
  artifacts:
    - path: components/ui/alert-dialog.tsx
      provides: Radix AlertDialog primitives styled like other ui/*
    - path: app/transactions/components/delete-transaction-alert.tsx
      provides: Named delete confirmation before API
    - path: app/transactions/components/add-transaction-dialog.tsx
      provides: Create + edit modes per D-01/D-03
    - path: app/transactions/hooks/use-transactions.ts
      provides: delete without confirm; toast validation
    - path: app/api/transactions/[id]/route.ts
      provides: PUT/DELETE proxy parity with categories/[id]
    - path: lib/api/transactions.ts
      provides: updateTransaction/deleteTransaction parity with lib/api/categories.ts
  key_links:
    - from: delete-transaction-alert.tsx
      to: useTransactions.deleteTransaction
      via: onConfirm invokes parent callback that calls deleteTransaction(id)
    - from: add-transaction-dialog.tsx
      to: useTransactions.updateTransaction
      via: onUpdate(id, TransactionInput) wired from income/expense clients in plan 02
---

<objective>
Deliver **API + shared UI** foundation for Phase 1001 **before** list wiring: (1) align **transaction** **`updateTransaction` / `deleteTransaction`** client helpers and **`app/api/transactions/[id]`** **PUT/DELETE** handlers with the **category** reference (per **D-06**); (2) add **AlertDialog**; (3) remove **`window.confirm`** / **`alert()`** from **`useTransactions`** in favor of **Sonner**; (4) extend **`AddTransactionDialog`** for **edit**; (5) add **`DeleteTransactionAlert`**.

Purpose: UI work plan 02 calls stable, documented API behavior that matches an established repo pattern.
Output: Updated `lib/api/*`, `app/api/transactions/*`, `components/ui/`, `app/transactions/*`; `npm run build` green.
</objective>

<execution_context>
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/workflows/execute-plan.md
@C:/Users/micha/Desktop/Codebase/Personal/finance-tracker-ui/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-CONTEXT.md
@.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-RESEARCH.md
@.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-UI-SPEC.md
@.cursor/rules/finance-tracker-agents.mdc
@app/api/categories/[id]/route.ts
@app/api/categories/common/utils.ts
@lib/api/categories.ts
@lib/api/endpoints/categories.ts
@app/api/transactions/[id]/route.ts
@app/api/transactions/route.ts
@lib/api/transactions.ts
@lib/api/endpoints/transactions.ts
@app/transactions/types/transaction.api.ts
@app/transactions/hooks/use-transactions.ts
@app/transactions/components/add-transaction-dialog.tsx
@components/ui/button.tsx
@components/ui/dialog.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Transaction update/delete API — align with category pattern (lib + Next routes)</name>
  <files>lib/api/transactions.ts, lib/api/endpoints/transactions.ts, app/api/transactions/common/utils.ts, app/api/transactions/route.ts, app/api/transactions/[id]/route.ts</files>
  <read_first>
    - `app/api/categories/[id]/route.ts` (PUT/DELETE structure and error JSON shape)
    - `app/api/categories/common/utils.ts` (shared validation pattern)
    - `lib/api/categories.ts` (`updateCategory`, `deleteCategory`, `apiFetch` usage)
    - `lib/api/endpoints/categories.ts` (JSDoc / endpoint constant style)
    - `app/api/transactions/[id]/route.ts` (full file)
    - `app/api/transactions/route.ts` (full file — shared date/category validation)
    - `lib/api/transactions.ts` (full file)
    - `lib/api/endpoints/transactions.ts`
    - `app/transactions/types/transaction.api.ts` (`UpsertTransactionRequest` fields)
  </read_first>
  <action>
    Per **1001-CONTEXT D-06**:

    1. Add **`app/api/transactions/common/utils.ts`**. Move or centralize validation helpers used by transaction routes (at minimum **`isValidTransactionDate`**; include **`isValidCategoryTypeQuery`** or equivalent for GET query parsing if that logic currently duplicates `route.ts`). Export only what both `route.ts` and `[id]/route.ts` need.

    2. Refactor **`app/api/transactions/route.ts`** and **`app/api/transactions/[id]/route.ts`** to **`import`** those helpers from `./common/utils` (or `../common/utils` as appropriate). Remove duplicated inline copies of the same validators.

    3. **`PUT` in `[id]/route.ts`**: After parsing/validating the request body as upsert fields (`UpsertTransactionRequest`), build an **explicit JSON body object** for the backend with only the fields the API expects (mirror category PUT which sends `{ name, categoryType }` only — for transactions send normalized fields: `name`, `categoryId`, `description`, `amount`, `transactionDate`, `frequencyId`, `createdBy`, consistent with `UpsertTransactionRequest`). Do **not** forward arbitrary unknown keys from the raw client payload.

    4. **`DELETE` in `[id]/route.ts`**: Keep parity with **`app/api/categories/[id]/route.ts`** — id trim check, `fetch` to `${API_URL}/v1/transactions/${id}`, map non-OK to `Response.json({ error: ... }, { status })`, success returns JSON with a **transaction-specific** success `message` (same pattern as categories).

    5. **`lib/api/transactions.ts`**: Ensure **`updateTransaction`** and **`deleteTransaction`** mirror **`updateCategory`** / **`deleteCategory`**: same `apiFetch` call shape, `PUT` with `Content-Type: application/json`, `DELETE` without body. Add brief comments if categories file uses file-level documentation.

    6. **`lib/api/endpoints/transactions.ts`**: Add the same style of **file / property JSDoc** as `lib/api/endpoints/categories.ts` so `list` / `byId` / `byCategoryType` are documented as Next.js-relative routes.

    7. Do **not** change the public TypeScript signatures of `updateTransaction`/`deleteTransaction` unless required for correctness.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `app/api/transactions/common/utils.ts`.
    - Both `app/api/transactions/route.ts` and `app/api/transactions/[id]/route.ts` contain the substring `common/utils` in an import path.
    - `app/api/transactions/[id]/route.ts` contains `method: "PUT"` and a `JSON.stringify(` call whose argument is not the bare identifier `body` alone (normalized payload object required).
    - `lib/api/transactions.ts` contains both `method: "PUT"` and `method: "DELETE"` in `apiFetch` calls targeting `TRANSACTION_ENDPOINTS.byId`.
    - `lib/api/endpoints/transactions.ts` contains a block comment `/**` documenting the endpoint constants (parity with `lib/api/endpoints/categories.ts` style).
  </acceptance_criteria>
  <done>
    Transaction update/delete stack matches category conventions; routes dedupe shared validators; build passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add shadcn AlertDialog primitive + Radix dependency</name>
  <files>package.json, package-lock.json, components/ui/alert-dialog.tsx</files>
  <read_first>
    - `package.json` (dependencies; confirm `@radix-ui/react-alert-dialog` absent)
    - `components/ui/dialog.tsx` (copy patterns: `cn`, forwardRef, class names)
    - `components/ui/button.tsx` (buttonVariants / Slot usage if alert-dialog needs Button)
  </read_first>
  <action>
    1. Run `npm install @radix-ui/react-alert-dialog` (adds runtime dep; lockfile updates).
    2. Create `components/ui/alert-dialog.tsx` following the same structure as official shadcn/ui alert-dialog: `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction`, `AlertDialogCancel`, using `cn` from `@/lib/utils`, `@radix-ui/react-alert-dialog`, and project `button` variants for Cancel/Action where applicable.
    3. Export all named pieces from the single file (match how `dialog.tsx` exports).
    4. Do not add a second global Toaster; Sonner stays as today per Phase 1000.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "@radix-ui/react-alert-dialog" package.json` matches at least one line.
    - `components/ui/alert-dialog.tsx` contains the substring `AlertDialogContent`.
    - `components/ui/alert-dialog.tsx` contains the substring `@radix-ui/react-alert-dialog`.
  </acceptance_criteria>
  <done>
    AlertDialog component file exists; dependency recorded; production build succeeds.
  </done>
</task>

<task type="auto">
  <name>Task 3: useTransactions — remove browser confirm/alert; use Sonner for validation</name>
  <files>app/transactions/hooks/use-transactions.ts</files>
  <read_first>
    - `app/transactions/hooks/use-transactions.ts` (full file)
    - `hooks/use-optimistic-list.ts` (deleteItem/updateItem toasts — avoid duplicating success toasts in callers)
  </read_first>
  <action>
    Per **D-04** and **1001-RESEARCH.md** (no double confirmation):
    1. Add `import { toast } from "sonner";` at top (module is already `"use client"`).
    2. In `addTransactionHandler` and `updateTransactionHandler`, replace every `alert("...")` with `toast.error("...")` using the same user-visible strings (or slightly improved copy matching Add dialog tone).
    3. In `deleteTransactionHandler`, remove the `confirm(...)` gate entirely: always call `deleteItem(id)` when invoked. Rely on **UI-layer** `AlertDialog` (plan 02 / `DeleteTransactionAlert`) so the user never deletes without confirming.
    4. Do not change `UseTransactionsResult` method names (`addTransaction`, `updateTransaction`, `deleteTransaction`) so `use-income-transactions` / `use-expense-transactions` stay compatible.
    5. Optional: improve fetch failure path with `toast.error` instead of only `console.error` (if you touch that block); keep `console.error` for diagnostics.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "confirm\\(" app/transactions/hooks/use-transactions.ts` returns no matches.
    - `rg "alert\\(" app/transactions/hooks/use-transactions.ts` returns no matches.
    - `app/transactions/hooks/use-transactions.ts` contains `from "sonner"`.
  </acceptance_criteria>
  <done>
    Delete path is callable without browser confirm; validation uses Sonner; TypeScript build passes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Edit-capable transaction dialog + DeleteTransactionAlert</name>
  <files>app/transactions/components/add-transaction-dialog.tsx, app/transactions/components/delete-transaction-alert.tsx</files>
  <read_first>
    - `app/transactions/components/add-transaction-dialog.tsx` (full file)
    - `app/transactions/types/transaction.model.ts` (`Transaction` interface for edit seed)
    - `components/ui/alert-dialog.tsx` (from Task 2)
  </read_first>
  <action>
    **A. `AddTransactionDialog` — create + edit (per D-01, D-03, UI-SPEC)**

    1. Extend props (keep backward-compatible defaults):
       - `mode?: "create" | "edit"` default `"create"`.
       - `transaction?: Transaction | null` — when `mode === "edit"` and dialog opens, seed fields from this object (`name`, `categoryId`, `description`, `amount`, `transactionDate` → date input, `createdBy` if needed for payload).
       - `onUpdate?: (id: string, input: TransactionInput) => void` — required when parent uses edit mode (TypeScript: document in prop JSDoc; at runtime parent must pass when `mode === "edit"`).

    2. Titles/subtitles per UI-SPEC: create keeps "Add Income/Expense"; edit uses **"Edit income" / "Edit expense"** (match casing tone of UI-SPEC) and a short subtitle.

    3. When `open` becomes true with `mode === "edit"` and `transaction`, populate state in a `useEffect` keyed on `open`, `mode`, `transaction?.id` (same pattern as reset on close). When switching to create, keep existing `reset()` behavior.

    4. Submit handler:
       - **create:** unchanged flow — `onSubmit(payload)`, success toast, reset, close.
       - **edit:** validate like create (toast.error messages); build `TransactionInput` including `categoryName` from `categoryById.get(categoryId)` and `createdBy` from `transaction.createdBy` or default `"finance-tracker-ui"`; call `onUpdate(transaction.id, payload)`; on success close dialog and reset (do **not** add a second success toast if `useOptimisticList` already toasts on update — mirror whether create currently double-toasts; prefer **no** extra `toast.success` in edit path if the hook layer already succeeds).

    5. Primary button label: **Save** for both modes or **Save** for edit / keep existing for create — UI-SPEC says Save for edit; create can stay "Save" as today.

    **B. `DeleteTransactionAlert` (new file)**

    1. Client component using `AlertDialog` from `@/components/ui/alert-dialog`, props: `open`, `onOpenChange`, `transactionName: string`, `onConfirm: () => void` (parent passes `() => { deleteTransaction(id); onOpenChange(false); }` or closes after delete).

    2. Copy per UI-SPEC: title **Delete transaction?**, description includes **transaction name** and irreversible warning.

    3. **Cancel** = outline/secondary; **Delete** = `Button` variant `destructive` inside `AlertDialogAction` (avoid nested buttons; follow Radix alert-dialog patterns).

    4. `onConfirm` runs only when user confirms; no API call until then.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `app/transactions/components/add-transaction-dialog.tsx` contains substring `mode` and `onUpdate` (prop names).
    - `app/transactions/components/delete-transaction-alert.tsx` exists and contains `AlertDialog` and `Delete transaction`.
    - `rg "export function DeleteTransactionAlert" app/transactions/components/delete-transaction-alert.tsx` matches.
  </acceptance_criteria>
  <done>
    Edit dialog and delete alert components compile; create-mode Add flow still works at typecheck level; build passes.
  </done>
</task>

</tasks>

<verification>
`npm run build` passes after all tasks; transaction API layer matches category pattern; no `window.confirm` / `alert` in `use-transactions.ts`; new UI files exist.
</verification>

<success_criteria>
Plan 01 completes API parity (D-06), AlertDialog primitive, hook behavior aligned with D-03/D-04, parameterized dialog for edit, reusable delete confirmation shell.
</success_criteria>

<output>
After completion, create `.planning/phases/1001-complete-income-and-expense-transaction-crud/1001-01-SUMMARY.md`
</output>
